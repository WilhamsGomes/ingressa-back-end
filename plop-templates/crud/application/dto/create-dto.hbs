import { IsOptional, IsString } from 'class-validator';

export class Create{{pascalCase featureKebab}}Dto {
  // Coloque seus campos aqui (ou evoluímos o plop para gerar via fields)
  // @IsString()
  // name: string;

  {{#if hasPassword}}
  @IsString()
  {{passwordField}}: string;
  {{/if}}

  @IsOptional()
  @IsString()
  example?: string;
}
