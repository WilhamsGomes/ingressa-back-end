import { Body, Controller, Delete, Get, HttpCode, HttpStatus, Param, Patch, Post } from '@nestjs/common';
import { ApiBadRequestResponse, ApiBody, ApiCreatedResponse, ApiNoContentResponse, ApiNotFoundResponse, ApiOkResponse, ApiOperation, ApiParam, ApiTags } from '@nestjs/swagger';

import { Create{{pascalCase featureKebab}}Dto } from '../../application/dto/create-{{kebabCase featureKebab}}.dto';
import { Update{{pascalCase featureKebab}}Dto } from '../../application/dto/update-{{kebabCase featureKebab}}.dto';

import { Create{{pascalCase featureKebab}}UseCase } from '../../application/use-cases/create-{{kebabCase featureKebab}}.usecase';
import { FindAllUseCase } from '../../application/use-cases/find-all.usecase';
import { Find{{pascalCase featureKebab}}ByIdUseCase } from '../../application/use-cases/find-{{kebabCase featureKebab}}-by-id.usecase';
import { Update{{pascalCase featureKebab}}UseCase } from '../../application/use-cases/update-{{kebabCase featureKebab}}.usecase';
import { Delete{{pascalCase featureKebab}}UseCase } from '../../application/use-cases/delete-{{kebabCase featureKebab}}.usecase';

@ApiTags('{{routeName}}')
@Controller('{{routeName}}')
export class {{pascalCase featureKebab}}Controller {
  constructor(
    private readonly createUseCase: Create{{pascalCase featureKebab}}UseCase,
    private readonly findAllUseCase: FindAllUseCase,
    private readonly findByIdUseCase: Find{{pascalCase featureKebab}}ByIdUseCase,
    private readonly updateUseCase: Update{{pascalCase featureKebab}}UseCase,
    private readonly deleteUseCase: Delete{{pascalCase featureKebab}}UseCase,
  ) {}

  @Post()
  @ApiOperation({ summary: 'Create {{pascalCase featureKebab}}' })
  @ApiBody({ type: Create{{pascalCase featureKebab}}Dto })
  @ApiCreatedResponse({ description: 'Created successfully' })
  @ApiBadRequestResponse({ description: 'Validation error' })
  async create(@Body() dto: Create{{pascalCase featureKebab}}Dto) {
    return this.createUseCase.execute(dto);
  }

  @Get()
  @ApiOperation({ summary: 'List {{routeName}}' })
  @ApiOkResponse({ description: 'List returned successfully' })
  async findAll() {
    return this.findAllUseCase.execute();
  }

  @Get(':id')
  @ApiOperation({ summary: 'Get {{pascalCase featureKebab}} by id' })
  @ApiParam({ name: 'id', type: String })
  @ApiOkResponse({ description: 'Found successfully' })
  @ApiNotFoundResponse({ description: 'Not found' })
  async findById(@Param('id') id: string) {
    return this.findByIdUseCase.execute(id);
  }

  @Patch(':id')
  @ApiOperation({ summary: 'Update {{pascalCase featureKebab}}' })
  @ApiParam({ name: 'id', type: String })
  @ApiBody({ type: Update{{pascalCase featureKebab}}Dto })
  @ApiOkResponse({ description: 'Updated successfully' })
  @ApiBadRequestResponse({ description: 'Validation error' })
  @ApiNotFoundResponse({ description: 'Not found' })
  async update(
    @Param('id') id: string,
    @Body() dto: Update{{pascalCase featureKebab}}Dto,
  ) {
    return this.updateUseCase.execute(id, dto);
  }

  @Delete(':id')
  @ApiOperation({ summary: 'Delete {{pascalCase featureKebab}}' })
  @ApiParam({ name: 'id', type: String })
  @ApiNoContentResponse({ description: 'Deleted successfully' })
  @ApiNotFoundResponse({ description: 'Not found' })
  @HttpCode(HttpStatus.NO_CONTENT)
  async delete(@Param('id') id: string): Promise<void> {
    await this.deleteUseCase.execute(id);
  }
}
